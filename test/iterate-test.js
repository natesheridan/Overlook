import chai from 'chai';
import User from '../src/js/user/User';

const expect = chai.expect;


let testUser1;
let testUser2;
let testUser3;
let sampleData = [
]

describe('Iterator Tests', function() {
  beforeEach(function() {

  });
  it('Should be able to find all of a users bookings', function() {
  });
  it('Should be able to find all of a users bookings and sum the total of the bookings', function() { 
  });
  it('should be able to find all available rooms in the hotel on a given date', function() {
  });
  it('should be able to filter the available rooms in the hotel by room type', function() {
  })
});